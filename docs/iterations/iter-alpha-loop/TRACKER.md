# Shamshir — iter-alpha-loop Tracker (resume here)

**This is the machine-readable progress source.** The narrative docs
(`PLAN.md`) stay the human authority — this file is the strict checkpoint table +
handoff.

**Read order for a fresh session:** this file → `PLAN.md` → `LEDGER.md` →
`../../../AGENTS.md` → `../../../conductor-DEBT.md` → `../../reference/SYSTEM-REFERENCE.md`
(+ CODE-MAP, BACKTEST-ARCHITECTURE, TEST-ARCHITECTURE) → `../../../DECISIONS.md`.
**Branch:** `iter/alpha-loop`.

## Handoff  (overwrite this block, ≤12 lines, no history)

last: **R3 session 1 DONE.** 12 pre-registered variants (pack/risk-profile swaps on 6 R1' top-20 cells) run+scored: gate PASS, 11/12 scored. 2 real wins — `ema-alignment/EURJPY/H1`+`runner-aggressive` (ExpectancyR +82%) and `trend-breakout/XAUUSD/H4`+`runner-aggressive` (+32%), both DD/Consistency-neutral. `scalp-tight` lost on all 3 cells tried; `conservative` risk never cleanly risk-adjusted-positive. Full writeup `evidence/scoreboard-s2.md`. App launched via `dotnet run` (ASPNETCORE_ENVIRONMENT=Development, port 5134) — still running.
stage: **R0–R2 (as P4), P0–P4, X0–X5, R1', F59, F60, R3.1 ALL DONE → R3.2 (walk-forward the best 3) is next.**
gate: R3.1 truth gate PASS (ExperimentRuns +12 exactly, every pre-registered variant scored-or-null, artifact committed).
next: (1) **R3.2**: walk-forward v6a/v1a/v6b (6 rolling windows, train 60d/test 30d) to get a real OOS ratio — first needs each strategy's tunable indicator `ParamGrid` looked up for `POST /api/walk-forward/start` (not yet done). (2) Two unchased observations from R3.1 worth a look if time allows: `scalp-tight`'s trade-count drops (mechanism unconfirmed) and v4b's trade collapse under `aggressive` risk (plausibly a DD-circuit-breaker interaction, unconfirmed). (3) F48 open (tape-vs-venue levels on XAUUSD-class symbols, non-blocking for tape-vs-tape ranking). (4) Minor: stale `completedAtUtc: null` on live monitor endpoint for an evicted run; orphan `96fa9214` experiment row cleanup (both harmless, optional).

## Checkpoints

Status ∈ TODO · IN PROGRESS · DONE · BLOCKED. Evidence = an artifact path produced by a run this
phase (a code path is not evidence).

| # | Checkpoint | Status | Commit | Evidence |
|---|-----------|--------|--------|----------|
| R0.0 | Session setup — housekeeping, AGENTS.md, TRACKER.md, ctrader-quickstart F21, PLAN.md fixes | DONE | — | AGENTS.md + ctrader-quickstart.md updated; TRACKER.md + LEDGER.md created |
| R0.1a | F20 — CTraderListenService.cs → DbPathResolver | DONE | — | CTraderListenService.cs:105 now uses `DbPathResolver.ResolveTradingDbPath()` |
| R0.1b | F21 — GET /api/system/health endpoint + doc fixes | DONE | — | SystemController.health returns `{status, dbPath, version}`; quickstart port →5134, kill-by-PID |
| R0.1c | F19 — barrier false-positive: scope to ctrader venue only | DONE | — | BacktestOrchestrator.cs:522 barriers only on venue=ctrader |
| R0.1d | F18 — compare-both child spawn: write start record at spawn, keep in _runs | DONE | — | RunCompareBothAsync writes WriteStartRecordAsync immediately; _runs.TryRemove removed from finally |
| R0.2a | research score verb + scoreboard + API | DONE | — | SetupScoreService + POST /api/experiments/score + GET /api/experiments/{id}/scoreboard + CLI verbs |
| R0.2b | research doctor verb + API | DONE | — | GET /api/system/doctor + `research doctor` CLI verb |
| R0.2c | DataQuality market-hours aware | DONE (VERIFIED-EXISTING) | — | SqliteMarketDataStore.StraddlesWeekend already filters FX weekend gaps since P6.1 |
| R0.truth | Truth gate: live verify tape run validate --forbid-warnings | DONE | — | Tape run f353fbd7: status=completed, exitCode=0, warnings=null, 5 trades. Doctor: PASS 0 issues. Health: PASS. Score: PASS on 2c9551d1 (28td, composite=68.5). All endpoints live-verified. |
| R0.gates | Gate battery: build + Unit + Integration + Sim-fast + golden | DONE (RE-VERIFIED) | — | 0err/5warn · 716/0/6 · 121/0/0 · 144/0/0 · golden 61/0/0 |
| R0.qa | QA session: static analysis + fix + refactor | DONE | — | 6 findings (SA-1 through SA-6): 5 fixed (untracked file, dead code, silent catch, bogus sort, namespace inconsistency), 1 observed (WarningsJson string check). All fixes compile + gates re-run clean. |
| R1.0 | StrategyId filter: extend scoring for per-strategy cells | DONE | — | SetupScoreService.ScoreRunAsync now accepts optional strategyId; ScoreRequest.StrategyId added; dedup uses VariantLabel+ExperimentId+BacktestRunId |
| R1.1 | Batched sweep: 28 tape runs (14 sym x {H1,H4}) all 9 strategies | DONE | — | 28 runs completed, 0 warnings, 31 min wall time. All runs serialize correctly with per-strategy TradeResult rows. |
| R1.2 | Score all 252 cells against baseline-sv1 experiment | DONE | — | 4 scored (>=20 trades), 248 below-floor, 0 failed. 100% coverage. 4 ExperimentRuns in DB. |
| R1.3 | Scoreboard artifacts | DONE | — | evidence/scoreboard-s1.md + evidence/scoreboard-s1.csv committed. Top: XAUUSD/H4/trend-breakout=100.0 |
| R2.0 | Audit fixes (C1, S1, S2, S5) — dead code, FoldRole, variantLabel, UpdatedAtUtc | DONE | — | SetupScoreService.cs, ExperimentRunEntity.cs, BacktestOrchestrator.cs |
| R2.1 | Compare-both configs: 6 configs created for top-3 cells x 2 windows | DONE | — | config/compare-both/*.json (6 new files) |
| R2.2 | Parity guard runs: 6 compare-both executed (3 cells x 2 windows) | DONE | — | 6 tape+6 cTrader runs, all terminal, no stuck runs, F18/F19/B1-3 verified |
| R2.3 | Reconcile: all 6 pairs reconciled; V4 table filled | DONE | — | docs/audit/RECONCILE-FINDINGS.md V4 table; 1 cell tradable (1:1 count, $271 delta per F1+F2) |
| R2.4 | Owner gate: BLOCKED (1 cell 33%); agent recommends PROCEED | DONE | — | RECONCILE-FINDINGS.md Owner gate block; F22/F23 filed |
| R2.5 | Gate battery re-verified | DONE | — | 0err/5warn · 716/0/6 · 121/0/0 · 144/0/0 · golden clean |
| R2.6 | v4: 2-month windows — XAUUSD-tb (14v15, 7.1%) + USDCAD-tb (13v13, 0%) | DONE | — | XAUUSD: 9f0ea5e5/197598ab; USDCAD: e29c5dfe/00aaba6a; divergence convergent at scale |
| R2.7 | Divergence investigation document | DONE | — | docs/iterations/iter-alpha-loop/R2-DIVERGENCE-INVESTIGATION.md (full trace + methodology + recommendations) |
| P0 | Cost-sign truth: unified negative convention, cBot partial-close fix, invariant tests | DONE | de52441 | Costs unified (Net=Gross+Comm+Swap); TradeCostCalculator+TradeResultFactory+TradingEngineCBot fixed; 2 invariant tests; 4 sign tests updated; gate 721/0/6·121/0/0·144/0/0 |
| P1 | Venue-declared symbol specs: cBot symbol_spec, VenueSymbolSpec entity, correct commission model, half-at-open | DONE | 393ff67 56871de 83519da | M51 migration; cBot emits specs for all subscribed symbols; CTraderBrokerAdapter parses + upserts registry; CommissionType dispatch (AbsPerLot/UsdPerMillion); half-at-open in all 3 adapters; per-trade reconcile deltas; gate 721/0/6·121/0/0·144/0/0 |
| P0+P1.QA | Static audit + LIVE cTrader verification; found+fixed F24 (MaxSlPips collapse, 100% signal rejection); filed F25-F29 | DONE | b418a98 | evidence/p1-symbol-specs.md; live repro RunIds e907e647/921ce1e4 (broken) -> f22e51bb/261bb748 (fixed); gate re-verified 721/0/6·121/0/0·144/0/0 |
| P2 | Limit-entry parity: resting-order contract doc + F30 (fine-bar expiry decrement) + F31 (cBot fill-reporting) + D11 flip | DONE | 15c7f85 | docs/reference/RESTING-ORDER-CONTRACT.md; evidence/p2-limit-entry-parity.md; RestingOrderContractTests.cs (4 tests); live repro a59183c1/02c56355 (0 ctrader trades) -> 26664e81/438b5977 (12/12); gate 725/0/6·121/0/0·144/0/0 |
| P3 | Exit + spread parity: gap-through/exit-spread verified correct + F32 (spread-number mismatch, same class as P1/P2 gaps) | DONE | (pending) | evidence/p3-exit-spread-parity.md; TapeReplaySpreadOverrideTests.cs (3 tests); live repro da7b3427/7c2be39b (13/12, tighter matching spread); gate 728/0/6·121/0/0·144/0/0 |
| F38 | **Stale bar (MASTER)** — cBot published `bars.Last(1)`, one bar behind; every cTrader order placed a bar late, every limit already marketable on arrival | DONE | (this commit) | PARITY-TRUTH-3.md §1; cBot `barClock[]` measured 8h→**4h** on H4; EURUSD entries 18+ pips → **0.00–0.20 pips**, open timestamps identical; live 792829b1/d64d9488 |
| F40 | Rested order (`"Pending"`) unparseable → adapter dropped the whole `bar_result` batch; fallback arm would have booked it as a **zero-price fill** | DONE | (this commit) | `OrderState.Pending` + PositionTracker early-return before dedup; per-exec isolation in `HandleBarResult`; RestingOrderExecutionTests.cs (3 tests) |
| F34 | **Currency as a config value** — `Account:Currency`; `CrossRateStore` → USD-pivot table fed from market data (was 2 stale literals); commission converted to account currency | DONE | (this commit) | PARITY-TRUTH-3.md §3; **proved by flipping to EUR live** → lots **identical** on both venues (closes old "F6 sizing divergence"); commission 17% → **0.5%**; CrossRateTests.cs (14 tests) |
| P4 | **Parity as a permanent gate** — `research parity` verb + API + pre-registered tolerance budget + one VERDICT line, exit 1 on FAIL | DONE (gate built; reports FAIL, correctly) | (this commit) | ParityGateService.cs; PARITY-TRUTH-3.md §5. FAILs = EUR account (owner) + tape's optimistic stop fills. **Tolerances NOT widened** per PLAN §P4 |
| P4.1 | Account centralized + reverted to 5834367; `Account:Currency`=EUR; `CTraderConnectionOptions` DI wiring | DONE | (this commit) | `CTraderConnectionOptions.cs`; wired into BacktestOrchestrator, DownloadJobService, BacktestRunner; docs updated |
| P4.2 | Gap-through extended to bar-close-through-stop in both adapters + LogDebug instrumentation | DONE | (this commit) | `TapeReplayAdapter.cs:617-640`; `BacktestReplayAdapter.cs:422-454`; PARITY-TRUTH-3.md §4.2 updated |
| — | **Investigation method** written up as a normative reference for future agents/models | DONE | (this commit) | `docs/reference/INVESTIGATION-METHOD.md` — R1–R9, derived from why 4 sessions missed F33/F38 |
| P4.2r | **REVERTED** the "fill at the bar close" stop model — it was fitted to one trade and would fill a bar that dips through your stop then trends at the FAR END of the move, inflating every trend-day loss | DONE | (this commit) | PARITY-TRUTH-4 §1; replaced by the measured `VenueFillModel` |
| F43 | **Resting orders fill at the first breaching M1 tick, NOT at their own price** — stops fill THROUGH, limits fill BETTER. Plus the short-exit spread was counted TWICE (bar already shifted to the ask side for detection, then spread re-added to the fill) | DONE | (this commit) | `VenueFillModel.cs` shared by both replay adapters; **`VenueFillModelTests` pins it against SIX REAL cTrader fills** (EURUSD+XAUUSD, both directions, limit entry + SL + TP); PARITY-TRUTH-4 §1 |
| F44 | **Venue symbol specs never persisted** — captured in memory on the cTrader leg only, so the tape priced commission/swap off fabricated symbols.json (a EURUSD long *earning* 0.5/night that the broker *charges* 2.445 for) | DONE | (this commit) | `IVenueSymbolSpecStore` + `SqliteVenueSymbolSpecStore`; `EngineHostOptions.VenueSymbolSpecs` wired at BOTH orchestrator sites (the F34 trap); VenueSymbolSpecs table now populated |
| F45 | **Swap wrong three ways**: rate read as MONEY (it is PIPS — venue declares `SwapCalculationType=Pips`), then NEGATED (it is already signed: negative = trader pays), and Sat/Sun rollovers CHARGED (no broker finances a shut market — that is why Wednesday is triple) | DONE | (this commit) | `TradeCostCalculator`; **`VenueSwapModelTests` pins it against the venue's THREE REAL swap charges**; PARITY-TRUTH-4 §3 |
| F46 | **Closing commission billed at the ENTRY price** — each side must be billed on its own notional at its own price. Invisible on EURUSD (0.53%), worth ~10% on XAUUSD | DONE | (this commit) | `TradeCostCalculator`; `ClosingCommission_isBilledAtTheExitPrice_notTheEntryPrice` |
| F47 | **cTrader prices backtest commission at ONE reference spot, not per trade** — constant **-20.67 EUR/lot round-turn** across an 18% gold move; same lots at different prices → identical commission. **Their artifact, not ours — do NOT match it** (it makes a backtest's cost depend on WHEN it was run) | **OPEN — owner decision** | — | PARITY-TRUTH-4 §4b; ctrader `a66fe9c4` |
| **P4** | **PARITY GATE GREEN — EURUSD `VERDICT: PASS`, budget untouched** | **DONE** | (this commit) | tape `a89d37b5` / ctrader `e497806d`: TradeCount 3:3 · EntryPrice **0.0 ticks** · Lots exact · ExitPrice **100% within, 0.0 ticks** · Commission 0.53% · Swap 0.44% · NetPnL 0.45% |
| **P4-xau** | **XAUUSD (14 trades, price 3369→4039): prices/lots/swap ALL GREEN** — the fill + swap models generalise across scale untouched. Commission EXEMPT (F47, owner-accepted); NetPnL still fails on F48 | PARTIAL | (this commit) | tape `2c5a3f5e` / ctrader `a66fe9c4`: EntryPrice **0.0 ticks** · ExitPrice **100% within, 0.0 ticks** · Lots exact · Swap 1.37% · Commission EXEMPT · NetPnL 1.37% (F48) |
| F47ex | **F47 exemption is EARNED FROM VENUE DATA, not granted** — the gate fires it only when the venue's commission-per-lot stayed flat (<2%) while trade prices moved (>5%), needs ≥4 trades, and it prints the measured divergence + backs it out of NetPnL. A blanket exemption would hide a real commission bug forever | DONE | (this commit) | `ParityGateService.VenueCommissionIsPriceIndependent`; XAUUSD reports `[PASS] Commission EXEMPT (F47) — was ≤2% → 9.88%` with a loud NOTE |
| F48 | PnL currency conversion (see above) | **OPEN** | — | PARITY-TRUTH-4 §4b |
| X0 | **Run queue + concurrency** — bounded tape pool (max 3), serial cTrader lane, persisted Status+QueuePosition, SQLite busy retry, Angular queue display | DONE (delivered, untested) | (prior session) | M52 migration; BacktestOrchestrator integrated queue; RunQueue:MaxTapeConcurrency=3 |
| X1 | **Progress + status truth** — real bar count from market data, honest lifecycle timestamps | DONE (delivered, untested) | (prior session) | IMarketDataStore.CountBarsAsync; ResolveBarCount replaces EstimateBarCount |
| — | **Environment fix (unrelated to X0/X1):** `Microsoft.EntityFrameworkCore.Design` floating `10.*` resolved to 10.0.10, downgrade-conflicting with the rest of the stack pinned at 10.0.9 → build failed before any test could run | DONE | (this commit) | Pinned to `10.0.9` in Infrastructure.csproj + Web.csproj, matching sibling EF packages |
| F49 | **X0's own truth gate had never been run**: 5 truly-parallel tape starts (real concurrent HTTP, not `dotnet run`-serialized) → all 5 timed out or 500'd. Root cause: `ValidateTapeDataAsync`→`GetInventoryAsync` is a ~10-16s **full-table GROUP BY scan** of the 1.2GB MarketDataBars table, run on *every* `Start()` call; the old one-run-at-a-time 409 guard (removed by X0, correctly, since concurrency is the point) had been silently serializing these scans for free | DONE | (this commit) | `BootstrapMarketDataStore.GetInventoryAsync` now coalesces concurrent callers onto one shared, 20s-TTL-cached `Task` (all callers of the inventory, not just Start, benefit: DataQualityValidator/ReferenceScalePopulator/DataManagerController/SystemController doctor) |
| F50 | **Thread-pool starvation**: `Start(cfg)`'s `ResolveBarCount` called `store.CountBarsAsync(...).GetAwaiter().GetResult()` — sync-over-async on the request thread, invisible when only one Start() could ever run at a time, a real starvation risk the instant X0 allowed concurrent Start() calls | DONE | (this commit) | `Start()` now uses the calendar estimate as an instant placeholder + fire-and-forget `RefreshBarCountAsync` upgrades `state.BarsTotal` once the DB answers; the compare-both cTrader child (which X1 claimed but never actually wired) now properly `await`s `ResolveBarCountAsync` too |
| F51 | **Cancelling a queued run had no effect until a concurrency slot opened**: the dequeued-but-waiting `Task.Run` called `semaphore.WaitAsync(CancellationToken.None)` — the run's own cancel token was ignored while queued, so "cancel mid-queue" (the tracker's own recommended smoke test) silently did nothing until the run would have started anyway | DONE | (this commit) | `semaphore.WaitAsync(state.CancellationSource!.Token)` + an `OperationCanceledException` catch that finalizes as Cancelled without ever touching the semaphore (verified: cancel-while-queued now terminates in ~2s instead of waiting for 3 slow runs ahead of it) |
| F52 | **A cancelled run displayed as "failed"**: `RunStatusResolver.Resolve` derives status from `ExitCode`/`ErrorMessage` alone and has no way to express "cancelled" — any non-null `ErrorMessage` (including the queue/shutdown-cancel paths' own "Cancelled (...)" messages) falls out as `Failed`. Pre-existing in X0's shutdown-cancel path too, not just F51's new path | DONE | (this commit) | `WriteEndRecordAsync` now takes an explicit `status:` and persists it to the (X0-added but previously write-only) `Status` column; `RunQueryService.ResolveStatus` (single-run) and a new `PreferPersistedTerminalStatus` (list) prefer that column over the legacy resolver whenever it names a real terminal state; old pre-migration rows (`Status=""`) fall through unaffected |
| F53 | **`state.RunPlanJson` raced its own readers**: only set deep inside `RunAsync` (after acquiring a concurrency slot), never at initial state construction — invisible while `Start()` did the F50 blocking DB call first (incidental delay), but a live GET landing right after a fast `POST /api/runs` could now see `"[]"` before `RunAsync` ever ran | DONE | (this commit) | `RunPlanJson` set in `Start()`'s initial object initializer alongside every other immediately-available field; caught by `RunMetadataTests.RowRun_persists_and_surfaces_full_selection` going red after the F50 fix made `Start()` fast enough to expose the race |
| — | **Live concurrency proof** (X0's own truth gate, never run before this session): 5 truly-parallel `POST /api/runs` (tape, EURUSD H1) → all 200, identical `startedAtUtc`, first 3 finish together (~3.4s), other 2 wait for a slot and finish ~3s later, **all 5 byte-identical** (`netProfit=-48.05991389653`, 3 trades) | DONE | (prior commit) | Reproduced 2x post-fix (v3/v4 and final builds); cancel-mid-queue reproduced separately with a slow (speed=0.05) config to create a real queued window |
| X2 | **Runs page + notes + copy-run** — M53 `Notes` column (SetNotesAsync sole writer, no-clobber pinned by test), PATCH notes API, richer table (strategy/TF/score/duration/notes/filter), SignalR+poll liveness, full-plan copy-run (`?copyFrom`) + reuse-last-params | DONE (live-verified) | (this commit) | LEDGER 2026-07-15; live: run 875b390b row showed strategies/score=57.1/duration; liveness run 5b148b30 self-updated running→NN%→completed with page untouched; e2e x2-x3.spec 8/8 |
| F55 | **Duplicated runs VANISHED from the Runs page** — old grouping suppressed every `parentRunId` row and only re-emitted compare-pair members; `/duplicate` runs (lineage, no pair) never rendered, nor children whose parent aged out of the 50-run window | DONE | (this commit) | groupedRuns rewrite; e2e pins duplicate 7c94c235 visible; synthetic pair tag rendered child indented under parent (reverted after) |
| F56 | **Copy-run appeared dead on a cold cache** — builder awaited 4 sequential lookups incl. the F49 inventory scan (>10s cold) before prefilling; prefill needs none of them | DONE | (this commit) | prefill first, lookups parallel; prefill catch logs instead of swallowing |
| X3 | **Trade chart rework** — context window (padBars default 20, 20/50/100 selector), directional entry/exit arrow markers with price, SL stepped path from journal BREAKEVEN/TRAIL rows, TP as trade-window line, prev/next navigation (runId/prev/next/index/count on detail), report-card nav via expandedTradeId swap | DONE (live-verified) | (this commit) | StopPathParsingTests ×4; live: USDCAD short w/ 21-point stop path; e2e: canvas renders, next-click survives, padBars=50 refetch asserted |
| F57 | **"Meaningless lines" root cause (3 stacked defects)** — markerFor dropped `time` (arrows impossible); lightweight-charts v5 removed `series.setMarkers` (call threw, killing updateChart); vertical open/close lines used two points at ONE timestamp (library rejects) | DONE | (this commit) | candle-chart rebuilt on v5 `createSeriesMarkers`; no same-timestamp series |
| F58 | **Initial SL painted with the FINAL stop** — `TradeResults.StopLoss` is post-BE/trail (sat below a trailed short's entry = instant-stop-out lie); chart now uses `InitialStopLoss` (M34), falling back for pre-M34 rows | DONE | (this commit) | Live: initial 1.41081 above short entry 1.40656, path walks to exit |
| — | **Housekeeping:** `web-ui/tests/e2e/output/` was partially git-TRACKED (failure artifacts churned every run) → gitignored + untracked | DONE | c1a7477 | web-ui/.gitignore |
| DX-D1 | **E2E triage (iter-dx-speed D1, executed here on owner instruction):** 15 pre-existing failures → 8 KEEP (7 stale-test fixes + 1 real product gap: analyzer page was orphaned from the report header), 3 QUARANTINE (pre-redesign live-chain form flows, `test.fixme` + revival conditions), 5 DELETE (dead UI: iter-37 config preview, old pack dropdown+regime checkbox, top-nav Packs link, per-strategy pack phrase, SignalR console-log hook) | DONE | 665e4a2 | `web-ui/tests/e2e/KNOWN-FAILURES.md`; full suite exits 0: 31 passed / 17 skipped in ~30s (was 7 min + 15 failures); dx-speed PLAN status updated |
| X4.0 | **cTrader consolidation** — `CTraderProcessOwner`: dynamic loopback ports (kills hardcoded 15562/3), one shared bounded lane (`CTrader:ProcessOwner:MaxConcurrency` default 2, 1=serial) shared by backtest+download, owned-PID reaping. Both download path AND orchestrator migrated onto it; image-name reaper (cross-kill hazard under parallel/2nd-worktree) removed | DONE (live-verified) | (x4 branch) | LEDGER X4; live log `starting cTrader CLI ... on ports 62188/62189` + `CTRADER\|OWN\|pid=17720\|tag=download:...`; owned-PID reap left 0 orphan cli |
| X4.1 | **Persisted watchlist** — `MarketDataSyncCells` in `MarketDataDbContext` via idempotent `CREATE TABLE IF NOT EXISTS` (EnsureCreated is a no-op on the existing 1.2 GB DB). NOT a trading-DB migration → no conflict with X2/X3's M53/snapshot. Only durable sync state; work derived from live coverage each tick | DONE | (x4 branch) | table created on the copied 1.2 GB DB at startup; watchlist upsert/toggle/remove verified live |
| X4.2 | **Coverage + gap engine** — `MarketDataCoverageService` aggregates inventory across sources per (sym,tf), overlays watchlist, market-hours-aware status (up-to-date/stale/missing/disabled) + missing tail; `MarketHours` (FX weekend closure, crypto 24/7); `GET /api/data-manager/coverage` | DONE (live-verified) | (x4 branch) | live: 84 cells, all correctly "stale" (data ends Jul-5/6, now Jul-15), each with right [lastBar→now] fill range |
| X4.3 | **Auto-sync** — `AutoSyncService` (BackgroundService, on by default, inert until watchlist non-empty): each tick recomputes missing tail from live coverage and drives the consolidated download path; self-healing (idempotent, no stuck jobs). `POST /sync-now` on-demand; watchlist CRUD API | DONE (live-verified) | (x4 branch) | live: pinned EURUSD h1 → sync-now started 1 real download → +170 bars, lastBar Jul-5→Jul-15 |
| X4.4 | **Truthful UI** — Data Manager "Coverage & Auto-Sync" grid: per-cell status badge, Watch toggle (pin/unpin), "Sync all → latest", server-truth 5 s refresh | DONE | (x4 branch) | ng build green; grid renders coverage; toggle+sync wired to the new endpoints |
| X4.5 | **Live cTrader E2E + resilience** — proven against actual cTrader (creds seankiaa/5834367); found ctrader-cli hangs on exit after a complete record → added idle-watchdog (reap ~45 s after record goes quiet) + ingest-regardless-of-exit-code so a hung/timeout run still lands its bars | DONE (live-verified) | (x4 branch) | log `record idle 45s — reaping CLI` → `complete — 0 bars ingested (exit 0)`; 2nd sync finished autonomously in 63 s, 0 orphan cli |
| X4.m | **X4 merged into `iter/alpha-loop`** — conflicts only in this tracker's table + auto-generated build-info.ts; merged code live-verified in the MAIN worktree (the 14-symbol cTrader run below ran through the merged `CTraderProcessOwner`) | DONE | 1c0f49f | full battery post-merge: Unit 759/0/6 · Integration 128/0/0 · Sim-fast 144/0/0 |
| R1'.0 | **D13 persistence (the F5 fix)** — SetupScoreService returned early on below-floor/warnings/venue failures without persisting; now every validity-gate failure upserts an ExperimentRun row with `sv1-null` + NullReason; scoreboard counts `nullRuns` separately and no longer drops 0-composite cells; added the missing §2 `run status completed` gate (RunStatusResolver) | DONE (live-verified) | c4b4c1f | SetupScorePersistenceTests ×6; live: `score 6c1c5743` (ctrader venue) → FAIL row persisted `sv1-null` w/ reason; `score d81598b0` → PASS 25 persisted |
| R1'.1 | **Venue specs for ALL 14 sweep symbols** — only EURUSD/XAUUSD were persisted (F44 landed after P1's in-memory capture); registry fallback to fabricated symbols.json is SILENT when any other spec exists (F3 hazard, would have skewed 12/14 of the R1' census) → one 14-symbol cTrader run captured all 14 `symbol_spec`s | DONE (live-verified) | — | run 6c1c5743 (83s, 5 trades, only known-F23 warning, 0 orphan cli); `VenueSymbolSpecs` 2→14 symbols; tape GBPJPY d81598b0: `VENUE_SPECS_LOADED 14`, 0 SYMBOL_FALLBACK, swap -146.90 commission -377.11 · venue truth: XAUUSD long swap is a CHARGE (-2.0895) where symbols.json claimed a credit |
| R1'.d | **Data readiness for the sweep** — all 14 symbols × {H1,H4} verified in marketdata.db spanning 2025-07-04 → 2026-07-03+ (sweep window 2025-07-04→2026-05-05 fully covered) | DONE | — | sqlite coverage query, 28/28 cells present (5.8k-8.7k H1 bars, ~1.5k H4 bars each) |
| R1'.2 | **The baseline sweep itself** — experiment `baseline-sv1-prime` (075d5240), 252 one-cell-per-run tape runs, defaults, 3-in-flight via X0 queue, every terminal run scored (D13); gate: 100% scored-or-null, 252 ExperimentRuns ≥ 225 → PASS | DONE | (this commit) | `evidence/scoreboard-s1p.{md,csv}`; `VERDICT: PASS experimentId=075d5240… scored=74 null=178 total=252`; 1h42m wall, avg 68 s/run, 0 run failures |
| F59 | **`POST /api/experiments` zero-variant crash + row leak** — `ExperimentRunner` Add-then-`UpdateAsync`(new instance, same Id) = EF double-tracking throw; experiment row left in status "Running" (MarkFailed hits the same bug); orphan `baseline-sv1-prime` row `96fa9214` in DB | DONE | (this commit) | Root cause fixed in `SqliteExperimentRepository.UpdateAsync` (fetch-tracked-instance + `CurrentValues.SetValues`, preserving `CreatedUtc`) — not the caller, so both the completion path and `MarkFailed` are fixed at once; `ExperimentRepositoryTests` ×2 (same-DbContext repro); live-verified: `POST /api/experiments` with `variants: []` now returns 200 `completed` (was a 500 throw). Orphan `96fa9214` left as-is (0 ExperimentRuns attached, harmless) |
| F60 | **Drawdown score component DEAD — saturated at 100 for every cell**: `BacktestRuns.MaxDrawdownPct` stores a FRACTION, `ComputeDrawdownScore` maps it as PERCENT (≤3→100). Proof: net-losing run `18621a31` stores 0.0140 (impossible as percent, = 1.40%). Fix units then re-score the same 252 runs (upsert) before trusting fine ranking | DONE | (this commit) | `SetupScoreService.ScoreRunAsync` now scores/stores `MaxDrawdownPct * 100`; `SetupScorePersistenceTests.DrawdownScore_ReadsStoredFractionAsPercent` pins 1.4%→Drawdown=100 and 11.6%→Drawdown=0; all 252 `baseline-sv1-prime` runs re-scored via `POST /api/experiments/score` (74 PASS / 178 FAIL unchanged — floor gate untouched); `evidence/scoreboard-s1p.{md,csv}` regenerated — top-20 ranking unchanged, 18/74 scored cells shifted >0.05 pts, largest -4.1 (`mtf-trend/BTCUSD/H1` 47.1→43.0) |
| X5 | **`refactor/god-classes` merged into `iter/alpha-loop`** — `BacktestOrchestrator` 2,387→821 lines behind `IVenueRunner` (`Services/Runs/*` + `Services/Venues/*`), cBot split into partials, last concrete-adapter type-sniff removed. Merge gate (SURVEY.md): one live cTrader compare-both smoke, since `RunEngineNetMqAsync` physically moved into `CTraderVenueRunner` | DONE (live-verified) | (merge commit) | Post-merge credential-free gates match refactor branch's own baseline exactly (Unit 759/6, Integration 134, Architecture 6/8 pre-existing, Simulation 149/153 — 1 pre-existing failure actually improved from crash→clean-handshake). Live smoke: compare-both `xauusd-h4-tb-2m.json` (XAUUSD H4, 2mo) — tape run `94434f42` (14 trades, $5,952.99) vs cTrader run `1ab467cd` (14 trades, $6,030.94, 5 SL/9 TP, 536 equity snapshots, 0 gate-limit rejections) — matches within the known F48 ~1.3% tape-vs-venue band. First attempt (`eurusd-h1-1d.json`, 1-day) hit `BAR_STREAM_TIMEOUT` (documented gotcha, 0-trade window too short to matter) |
| R3.1 | **Refinement loop, session 1** — 12 pre-registered variants (pack swap or risk-profile swap, one knob at a time) on 6 cells from the R1' top-20; corrected PLAN.md's §R3 knob inventory first (packs are 3 fixed presets not tunables, "session filters via VenueSessions" doesn't exist — real mechanism is `ExitLabEvaluateRequest.Regime`) | DONE (live-verified) | (this commit) | `evidence/scoreboard-s2.md`; gate PASS — 12/12 ExperimentRuns persisted, 11 scored/1 null-with-reason (below floor). 2 real wins (both `runner-aggressive`: `ema-alignment/EURJPY/H1` ExpectancyR +82%, `trend-breakout/XAUUSD/H4` +32%, both DD/Consistency-neutral); `scalp-tight` lost on 3/3 cells tried; `aggressive` risk scale-invariant on 1 cell, degraded on 2; `conservative` risk never cleanly risk-adjusted-positive. Next: walk-forward best 3 (v6a, v1a, v6b), blocked on strategy `ParamGrid` lookup |

## Quick commands

```powershell
dotnet build TradingEngine.slnx
dotnet test tests/TradingEngine.Tests.Unit
dotnet test tests/TradingEngine.Tests.Integration
dotnet test tests/TradingEngine.Tests.Simulation --filter "RequiresCTrader!=true&Category!=E2E&Category!=Slow&Category!=NetMQ"
```
