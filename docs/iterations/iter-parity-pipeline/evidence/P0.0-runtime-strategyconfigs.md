# P0.0 — Runtime evidence: StrategyConfigs.OrderEntryJson (R5 propagation check)

Query run this phase against the RUNTIME store the Web app uses:

    sqlite3 src/TradingEngine.Web/data/trading.db \
      "SELECT Id, OrderEntryJson FROM StrategyConfigs ORDER BY Id;"

Result (2026-07-08, after Q1 JSON revert):

    bb-squeeze       | {"Method":0, "LimitOffsetPips":2, "LimitOrderExpiryBars":3, "LimitOffsetAtrFraction":null, ...}
    ema-alignment    | {"Method":0, ...}
    macd-momentum    | {"Method":0, ...}
    mean-reversion   | {"Method":1, "LimitOffsetPips":5, "LimitOrderExpiryBars":3, "LimitOffsetAtrFraction":0.25, ...}
    mtf-trend        | {"Method":0, ...}
    rsi-divergence   | {"Method":0, ...}
    session-breakout | {"Method":0, ...}
    super-trend      | {"Method":0, ...}
    trend-breakout   | {"Method":0, ...}

Method: 0 = Market, 1 = LimitOffset.

## Interpretation

1. R5 PASS for P0.0: the runtime store already holds Method=0 (Market) for the 8
   non-mean-reversion strategies and Method=1 (Limit) for mean-reversion. After the Q1
   revert, `config/strategies/*.json` now MATCHES the runtime — the JSONs no longer
   diverge from what a run actually executes (this was F9: JSON said LimitOffset, DB said
   Market, every run used Market). P0.0 introduces no propagation gap.

2. Companion-field drift is still present and is EXPECTED (F9/F7, not fixed here):
   DB has LimitOffsetPips=2, LimitOrderExpiryBars=3, LimitOffsetAtrFraction=null;
   the JSON companions are 3.0 / 12 / 0.15. The seeder seeds once and does not propagate
   JSON edits. This is inert while Method=Market (the LimitOffset companions are unused).
   P1.2 (config propagation + drift detection, content-hash upsert) closes this — that is
   the phase that must re-verify these companions propagate.
